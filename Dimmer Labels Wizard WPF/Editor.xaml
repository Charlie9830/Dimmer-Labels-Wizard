<Window x:Class="Dimmer_Labels_Wizard_WPF.Editor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Dimmer_Labels_Wizard_WPF"
        mc:Ignorable="d"
        Title="Editor" Height="900" Width="1200">
    
    <Window.DataContext>
        <local:EditorViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Border x:Name="LabelStripArea" BorderThickness="2" BorderBrush="Black" Grid.Row="0">
            <local:LabelStrip HorizontalAlignment="Center" VerticalAlignment="Center">
                <local:LabelStrip.Style>
                    <local:LabelStripTemplate/>
                </local:LabelStrip.Style>
            </local:LabelStrip>
        </Border>
        <Border x:Name="ControlsArea" BorderThickness="2" BorderBrush="Black" Grid.Row="1">
            <Grid>
                <TextBox x:Name="textBox" HorizontalAlignment="Left" Height="23" Margin="10,10,0,0" TextWrapping="Wrap" Text="{Binding Path=SelectedData}"  VerticalAlignment="Top" Width="120"/>
            </Grid>
        </Border>
    </Grid>
</Window>
